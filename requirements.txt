pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
scipy>=1.10

# optional but recommended -- pipeline degrades gracefully without them
pyarrow>=13.0        # parquet output for data/bronze,silver,gold (falls back to CSV)
xgboost>=2.0         # used in modeling.py if installed (falls back to HistGradientBoosting)

# dashboard
streamlit>=1.32
plotly>=5.18
